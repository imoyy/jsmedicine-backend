# 项目开发进度追踪

## 状态说明

- `[x]` 已完成：代码已实现并通过必要验证。
- `[~]` 进行中：当前阶段正在推进。
- `[ ]` 待开始：尚未进入实现。
- `[!]` 阻塞：存在依赖、需求或技术问题，需要先处理。

## 阶段 0：项目基础设施

状态：`[x]` 已完成

目标：建立可运行、可迁移、可扩展的后端基础工程。

- `[x]` 添加 Spring Boot Web、Validation、Security、AOP、Redis、Actuator 等基础依赖。
- `[x]` 添加 MySQL、Flyway、MyBatis-Plus、dynamic-datasource、springdoc-openapi。
- `[x]` 添加 Lombok、MapStruct、Hutool、Apache Commons 等通用依赖。
- `[x]` 使用 Docker Compose 管理 MySQL、Redis、MinIO、Nginx 等开发服务。
- `[x]` 配置多环境 `application` 文件和 `.env.example`。

验收标准：

- `[x]` `./mvnw -Dmaven.repo.local=.m2/repository test` 通过。
- `[x]` `./mvnw -Dmaven.repo.local=.m2/repository clean package -DskipTests` 通过。

## 阶段 1：数据模型与数据访问骨架

状态：`[x]` 已完成

目标：根据管理端使用手册建立实体优先的数据模型，并用 Flyway 管理数据库结构。

- `[x]` 设计并实现系统管理、账号管理、首页、课程、图书、资讯、播客、专题、专家、题库、统计、直播、答疑、反馈等核心表结构。
- `[x]` 为所有业务表建立 Java Entity，避免只写 SQL 而缺少代码模型。
- `[x]` 为所有可持久化实体建立 MyBatis-Plus Mapper。
- `[x]` 建立通用实体基类、审计字段、逻辑删除字段和状态枚举。
- `[x]` 增加 `tags`、`resource_tags`、`entity_extensions`，支持后续标签、筛选和动态扩展字段。
- `[x]` 编写数据库设计说明文档。

验收标准：

- `[x]` Flyway 初始化脚本可创建完整 schema。
- `[x]` Entity 与表名一一对应。
- `[x]` Entity 与 Mapper 一一对应。
- `[x]` Maven 测试与打包通过。

## 阶段 2：通用后端能力

状态：`[~]` 进行中

目标：补齐所有业务模块都会依赖的 API 基础能力。

- `[x]` 统一响应结构。
- `[x]` 全局异常处理。
- `[x]` MyBatis-Plus 分页插件配置。
- `[x]` 审计字段自动填充。
- `[x]` Spring Security 基线配置。
- `[ ]` 请求日志与操作审计切面。
- `[ ]` 参数校验错误码和错误响应规范细化。
- `[ ]` Swagger/OpenAPI 基础信息配置。
- `[ ]` API JSON 导出脚本或命令。

验收标准：

- `[ ]` 所有新增 Controller 默认返回统一响应结构。
- `[ ]` 常见异常场景有稳定错误码。
- `[ ]` Swagger UI 可访问。
- `[ ]` 最新 `api/api.json` 可生成。

## 阶段 3：认证与权限

状态：`[ ]` 待开始

目标：实现管理端登录、令牌认证、角色权限识别和权限校验。

- `[ ]` 实现管理员登录接口。
- `[ ]` 使用 BCrypt 校验密码。
- `[ ]` 实现 JWT 或等价 Bearer Token 机制。
- `[ ]` 实现认证过滤器和当前管理员上下文。
- `[ ]` 加载管理员角色与权限。
- `[ ]` 初始化超级管理员、基础角色和权限种子数据。
- `[ ]` 实现退出登录和登录状态校验。
- `[ ]` 覆盖登录、无权限、Token 失效等测试。

验收标准：

- `[ ]` `/api/v1/auth/login` 可用。
- `[ ]` 除公开接口外，所有接口默认需要认证。
- `[ ]` 权限不足返回稳定错误码。
- `[ ]` Swagger JSON 已导出到 `api/api.json`。

## 阶段 4：系统管理与账号管理

状态：`[ ]` 待开始

目标：优先实现后台管理端最基础的运营能力。

- `[ ]` 管理员管理：新增、修改、禁用、重置密码、分页查询。
- `[ ]` 角色管理：新增、修改、禁用、分页查询。
- `[ ]` 权限分配：角色绑定权限、管理员绑定角色。
- `[ ]` 用户管理：用户分页、详情、状态管理。
- `[ ]` 学员管理：学员分页、详情、学习信息维护。
- `[ ]` 操作审计记录查询。

验收标准：

- `[ ]` 系统管理接口具备权限校验。
- `[ ]` 分页参数统一为 `page`、`size`、`sort`。
- `[ ]` 核心 Service 和 Controller 测试通过。
- `[ ]` Swagger JSON 已更新。

## 阶段 5：内容与配置管理

状态：`[ ]` 待开始

目标：实现运营配置和内容审核相关功能。

- `[ ]` 首页分类管理。
- `[ ]` 首页内容配置。
- `[ ]` 资讯新增、编辑、删除、审核。
- `[ ]` 播客音频管理、配置、审核。
- `[ ]` 专题管理及课程、书本、学员、资讯、博客、题库等关联配置。
- `[ ]` 文件上传与媒体资源管理。

验收标准：

- `[ ]` 支持草稿、待审核、审核通过、审核拒绝等状态流转。
- `[ ]` 审核操作写入审计记录。
- `[ ]` 内容删除使用逻辑删除。
- `[ ]` Swagger JSON 已更新。

## 阶段 6：课程、图书与题库

状态：`[ ]` 待开始

目标：实现学习资源管理和考核配置。

- `[ ]` 课程列表、详情、新增、修改、审核。
- `[ ]` 课程视频配置。
- `[ ]` 图书分类管理。
- `[ ]` 图书列表、详情、新增、修改、审核。
- `[ ]` 图书章节管理。
- `[ ]` 题库分类管理。
- `[ ]` 题目新增、修改、删除。
- `[ ]` 题目选项维护。
- `[ ]` 考卷配置和题目关联。

验收标准：

- `[ ]` 课程、图书、题库支持分页和条件查询。
- `[ ]` 题目与考卷关系数据一致。
- `[ ]` 审核状态流转受权限控制。
- `[ ]` Swagger JSON 已更新。

## 阶段 7：专家、直播、答疑与反馈

状态：`[ ]` 待开始

目标：实现互动、服务和运营处理能力。

- `[ ]` 专家分类管理。
- `[ ]` 专家信息与履历管理。
- `[ ]` 直播新增、修改、删除、审核。
- `[ ]` 答疑问题查看、回复查看、删除。
- `[ ]` 反馈查看、处理、删除。

验收标准：

- `[ ]` 直播时间、状态和审核流转校验完整。
- `[ ]` 答疑与反馈删除使用逻辑删除。
- `[ ]` 关键操作写入审计记录。
- `[ ]` Swagger JSON 已更新。

## 阶段 8：统计管理

状态：`[ ]` 待开始

目标：实现管理端统计查询能力，优先保证查询正确性和可维护性。

- `[ ]` 学时统计。
- `[ ]` 学员统计。
- `[ ]` 地区统计。
- `[ ]` 成绩统计。
- `[ ]` 统计查询 SQL 明确落在 MyBatis XML 或注解中，避免 Service 内拼接复杂 SQL。
- `[ ]` 根据实际查询压力补充必要索引。

验收标准：

- `[ ]` 统计接口支持时间范围和必要筛选条件。
- `[ ]` 统计结果与基础明细数据一致。
- `[ ]` 覆盖关键统计测试。
- `[ ]` Swagger JSON 已更新。

## 阶段 9：联调、验收与上线前质量保障

状态：`[ ]` 待开始

目标：完成管理端联调、验收修复和上线前质量检查。

- `[ ]` 前后端接口联调。
- `[ ]` 统一检查接口响应结构、错误码和分页格式。
- `[ ]` 统一检查权限边界。
- `[ ]` 统一检查事务回滚和数据一致性。
- `[ ]` 统一检查日志、审计和敏感信息脱敏。
- `[ ]` 补充关键流程测试。
- `[ ]` 生成并归档最终 Swagger JSON。
- `[ ]` 完成生产配置模板检查。

验收标准：

- `[ ]` 核心业务流程可从管理端完整走通。
- `[ ]` `./mvnw test` 通过。
- `[ ]` `./mvnw clean package -DskipTests` 通过。
- `[ ]` `api/api.json` 代表最新接口契约。

## 当前优先级

1. 完成阶段 2 剩余通用能力。
2. 进入阶段 3，实现认证与权限闭环。
3. 认证稳定后优先推进阶段 4，保证后台管理端基础能力可用。

## 变更记录

- 2026-05-25：创建阶段化开发进度文档，记录阶段 0、阶段 1 完成状态，并明确后续实现顺序。
